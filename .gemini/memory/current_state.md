# Текущий статус проекта "MinecraftButBetter"

## Реализованные фичи
- **Stylized AAA Rendering Engine (Cinematic Survival Edition)**:
  - **Moody Lighting**: Switched to 2-band sharp toon shading with cool, atmospheric ambient light for a serious survival tone.
  - **Heavy Crease AO**: Deepened depth-based occlusion to emphasize world weight and geometry.
  - **Filmic Post-Stack**: Implemented 1.15 gamma contrast, subdued natural vibrance, and cinematic vignette.
  - **Atmospheric Fog**: Dense blue-grey fog for grounded horizon blending.
  - **Viewmodel Polish**: Balanced hand lighting to fit the moody atmosphere.
- **AAA Locomotion & Impulse Engine (v4.5)**:
  - **Item-Specific Animation System**:
    - **Data-Driven Lookup**: Replaced hardcoded animation names with a dynamic system. Items can now have unique walk, idle, and swing animations.
    - **AnimationComponent**: Added support for explicit animation overrides in item JSON files.
    - **Smart Fallback**: Implemented automatic fallback to type-specific animations (e.g., `axe_swing`, `block_walk`) based on `ToolType` or `isBlock()` status.
  - **Parkour Stability & Polish**:
    - **Ledge Grab Snapping**: Implemented automatic snapping to block faces and precise `baseYaw` alignment in `ParkourHandler` to eliminate wall clipping and jitter during diagonal grabs.
    - **Weighted Blending (Restored)**: Re-implemented smooth interpolation between locomotion and parkour states via `parkourWeight`.
  - **New Animation Assets**:
    - Added heavy-weight animations for blocks (`block_walk`, `block_idle`, `block_swing`).
    - Added aggressive swing animation for axes (`axe_swing`).
- **Texture Array Graphics Engine (v4.0) (NEW)**:
  - **GL_TEXTURE_2D_ARRAY Migration**: Completely eliminated texture shimmering and edge bleeding by switching from a 2D atlas to a layered Texture Array.
  - **3D Texture Coordinates (UVW)**: Updated all mesh generators (Chunk, Item, Carving) to use 3rd component for layer indexing.
  - **Smart UI Rendering**: Implemented dual-sampler logic in `UIRenderer` to support both standalone `sampler2D` (GUI, Text) and `sampler2DArray` (Block icons).
  - **Distance Quality Fixes**: 
    - Re-tuned `LOD_BIAS` to `-0.5` for sharper distance textures.
    - Implemented **4x Anisotropic Filtering** for clear surfaces at oblique angles.
    - Increased **Alpha Discard threshold to 0.5** in shaders to fix darkening of foliage/grass at a distance.
- **Modular Animation Engine (v3.5) (NEW)**:
  - **AAA Locomotion & Interpolation**: Implemented **Fixed-UPS/Interpolated-FPS** architecture (170 UPS logic) to eliminate jitter. Added `prevPosition` and `alpha` support for sub-frame smoothing.
  - **Full Body Representation**: Extended JSON format with `item_x/y/z` and `item_pitch/roll` tracks. Added `mirror` support for organic hand-alternation.
  - **Weighted State Blending**: Introduced `parkourWeight` (lerped at 18.0f) for seamless transitions between locomotion and parkour states.
- **Modular Physics & Parkour (v1.5) (NEW)**:
  - **Alternating Hands Logic**: Implemented strict side-toggling (`climbSide`) in `ParkourHandler`. Every new ledge grab alternates between left and right hands.
  - **Snappy Timings**: Re-tuned `grabbing.json` (0.25s) and `PhysicsSettings` (0.28s) for a weighted, responsive feel.
  - **Universal Accessibility**: Enabled ledge grabbing while flying to ensure consistency across all game modes.
- **Modular Animation Engine (v3.0)**:
  - **File-per-Animation Architecture**: Externalized animations into individual JSON files in `minecraft/animations/`.
  - **Animation Groups & Randomization**: Supported groups (e.g., `run_1`, `run_2`) with automatic random selection in `AnimationRegistry`.
  - **Looping Support**: Added `looping` flag for continuous animations (Idle, Walk).
  - **Procedural Mirroring**: Retained and improved the `mirrored` track logic for organic hand-alternation.
- **Survivor's Tablet (Дневник выжившего)**:
  - Полностью реализована модульная система обучения.
  - Современный карточный UI (50% ширины, 65% высоты экрана) с поддержкой прокрутки контента.
  - **ScrollPanel Component**: Универсальный UI-компонент с аппаратным отсечением (`glScissor`), решающий проблему "вылетающего текста".
  - **Dynamic Recipe Rendering**: Автоматическая отрисовка схем крафта прямо из игровых файлов.
    - Скалывание: отображение 5x5 сетки паттерна с циклической сменой допустимых материалов.
    - Пень: отрисовка ингредиентов, инструмента и счетчика ударов с текстовой тенью для читаемости.
  - Система категорий и статей полностью Data-Driven (JSON + `.index`).
- **Расширение In-World Crafting (Пень)**:
  - Добавлена поддержка нескольких ингредиентов (массив `ingredients`) в рецептах.
  - Реализован маппинг пустой руки на технический предмет `minecraft:hand`.
  - Добавлены/обновлены рецепты Tier 1: нити, заточенная палка, каменный топор, мешочек.
- **Гибкое скалывание (Napping)**:
  - Система переведена на поддержку списка входных материалов (`List<Identifier>`).
  - Все базовые инструменты (нож, топор, кирка, стрела) теперь можно делать как из кремня, так и из обычного камня.
- **Исправление системных ошибок**:
  - Исправлен баг "бессильной руки": теперь при пустой руке используется скорость 0.2, что позволяет ломать траву и медленно добывать землю/дерево.
  - Исправлен NPE в `StumpBlockDefinition` при клике пустой рукой.
- **Рефакторинг взаимодействия с миром (v2)**:
  - **Clean InputManager**: из `InputManager` удален хардкод логики для пней, ямного обжига, костров и генераторов. Логика перенесена в `onUse()` соответствующих `BlockDefinition`.
  - **Data-Driven Interactions**: введена поддержка типов блоков через `BlockTypeRegistry`. Новые типы: `campfire`, `pit_kiln`, `unfired_vessel`, `log`.
  - **Missing Assets Fallback**: введена система "шахматки" (пурпурный/черный) в `Texture.java`. При отсутствии ресурса игра не падает, а подставляет визуальный заглушку.
  - **Lazy Ticking Optimization**: внедрена система умного обновления `BlockEntity`. Сущности теперь могут сообщать `shouldTick() == false`, чтобы не нагружать `World.update`.
- **Универсальная Модульная Система Инвентаря (v3)**:
  - Внедрена полная абстракция данных через интерфейс `IInventory`.
  - **Data-Driven GUI**: все окна теперь описываются в JSON.
  - **CSS-like Layout System**: внедрена поддержка якорей (`anchor`) и гибкого выравнивания (`alignX`, `alignY`).
  - **Автоматическое Центрирование**: реализован расчет габаритов групп в `InventoryLayout` для идеального горизонтального позиционирования.
  - **Рефакторинг Инвентаря Игрока**:
    - Конфигурация: 9 (хотбар) + 8 (карманы 2x4) + 8 (снаряжение) + 8 (сумка) = 33 слота.
    - Исправлен баг `ArrayIndexOutOfBoundsException` (TOTAL_SIZE увеличен до 33).
    - Хотбар инвентаря идеально выровнен по горизонтали и поднят выше для предотвращения наложения на HUD.
  - **Developer Panel (Creative Menu) (v2.0)**:
    - Реализован плавный скроллинг через универсальный компонент `ScrollPanel`.
    - Добавлена поддержка `glScissor` для чистого отсечения иконок при прокрутке.
  - **Advanced Inventory Interactions (v3.2)**:
    - **Mouse Tweaks Style**: Реализован Shift-Drag для быстрого перемещения предметов и двойной клик для автоматического сбора стака.
    - **Hotbar Swapping**: Добавлена поддержка клавиш 1-9 для мгновенного перемещения предметов в хотбар.
    - **Data-Driven Stacking**: Внедрена поддержка `maxStackSize` в JSON предметов.
      - По умолчанию: блоки (128), инструменты (1), остальные (16).
      - Переопределено для: `pouch`, `fuel_canister`, `hand`, `fire_starter` (все по 1).
    - **Z-Order Fix**: Отрисовка предмета в руках и тултипов перенесена в самый конец, теперь они всегда поверх панелей.
    - **Return Logic**: Если кликнуть по тому же предмету в панели, который уже в руках, он "возвращается" (исчезает из курсора).
    - Исправлен баг с отсутствующими блоками (трава и др.) из-за лимита в 84 слота.
- **Универсальная Система Тинтинга (Universal Tinting)**:
  - Реализована поддержка тега `minecraft:tinted` в JSON блоков.
  - Шейдер `fragment.glsl` теперь использует знак `blockType` (отрицательный ID) для определения необходимости наложения тинта (трава, листва).
  - Удален хардкод UV-координат из шейдера и `Renderer.java`.
- **Stripped Logs (Обтёсанные бревна)**:
  - Добавлены обтёсанные варианты для всех типов деревьев (акация, береза, темный дуб, джунгли, ель).
  - Полная поддержка в `Blocks.java`, `.index` и JSON ресурсах.
- **Tier 1: Foraging & Scavenging**:
  - Реализована механика сбора 3D ресурсов с земли (палки, камни, кремень).
  - Внедрен `ResourceEntity` для рендеринга честных 3D-моделей предметов в мире через `ItemMeshGenerator`.
  - **Универсальный In-World Crafting API (v2)**:
    - Реализован интерфейс `ICraftingSurface` и `CraftingLayoutEngine`.
    - Поддержка адаптивной сетки 3x3 с фиксированными якорями.
    - Механика дискретных ударов (`isNewClick`).
    - Управление: ПКМ — положить, Shift+ПКМ — забрать, ЛКМ (с инструментом) — удар.
    - Система полностью Data-Driven.
    - Добавлена логика превращения бревна (`oak_log`) в рабочий пень (`stump`) при ударе топором.
  - Реализовано динамическое масштабирование (`visualScale`) предметов через JSON.
  - Реализован рейкаст по сущностям и мгновенный сбор ресурсов ЛКМ.
  - Настроена Data-Driven генерация ресурсов через `ScavengeDecorationStep` и `scavenge.json`.
  - **Система Хитбоксов**:
    - Реализована универсальная система генерации AABB на основе текстур (`TextureAABBGenerator`).
    - `ResourceEntity` теперь имеют идеально точные хитбоксы.
    - Физика коллизий (`Entity.move`) и выбор блоков (`Raycast`) теперь полностью учитывают `VoxelShape`.
  - Улучшен UX строительства: превью блоков теперь отображается только при зажатой клавише Shift.
  - Добавлены базовые блоки и предметы Tier 1: `unfired_vessel` и `fired_vessel` с кастомными хитбоксами.
  - Реализована честная система дропа: блоки выбрасывают `ItemEntity`.
  - Добавлена глина (`clay`) и комочки глины (`clay_ball`).
  - Реализована система вероятностного дропа: `dropChance` в JSON.
  - Добавлена трава (`short_grass`) и солома (`straw`).
  - **Высокая трава (Tall Grass & Double Plants)**:
    - Архитектурная поддержка через `PlacementType.DOUBLE_PLANT`.
    - Механика установки двух блоков и синхронного разрушения.
    - Умный рендеринг: поддержка `upperTexture`.
    - Настроена генерация в мире через `OvergrowthStep`.
  - **Исправление физики предметов (Weight Fix)**:
    - Исправлена ошибка ускорения легких предметов.
    - Внедрена универсальная формула затухания скорости (Clamp 0.1 - 0.98).
  - **Slot Ghost Icons (Placeholders) (NEW)**:
    - Внедрена система плейсхолдеров через JSON.
    - Реализован эффект Grayscale через фрагментный шейдер.
    - Поддержка полупрозрачных серых иконок для брони и сумок.
  - **Kinetic Inventory & SDF Octagons (NEW)**:
  - **SDF Shapes**: Реализована форма «индустриального октагона» со срезанными углами. *Временно отключено в коде (isSlot=0) для доработки баланса яркости.*
  - **Slot Ambient Light**: Разработана система внутреннего градиентного свечения в `ui_fragment.glsl` для подсветки темных предметов.
  - **Kinetic Hover**: При наведении на слот 3D блоки плавно вращаются, а 2D предметы «дышат».
  - **UIAnimationManager**: Система плавного затухания анимаций (LERP) при уходе курсора.
  - **Data-Driven Hotbar & UI Effects (NEW)**:

  - **JSON Configuration**: Хотбар полностью переведен на `hotbar.json` с поддержкой якорей и динамического выравнивания.
  - **UIEffectsRenderer**: Создан отдельный класс для визуальных эффектов (уголки выбора, пульсация, свечение), что позволило очистить `UIRenderer`.
  - **Dynamic Selection Styles**: Реализована система стилей выделения (brackets, border) с настройкой цвета, толщины и анимации прямо в JSON.
  - **HUD Logic**: Внедрен флаг `hudVisible` и автоматическое скрытие HUD-элементов при открытых окнах (инвентарь, меню).
  - **Clean Code**: Удалено более 100 строк хардкода и устаревшие текстуры `widgets.png`.
  - **3D Inventory Rendering & Visual Polish (v1.0) (NEW)**:
  - **3D Block Icons**: Реализован честный 3D-рендеринг блоков в инвентаре (кубы, ступеньки, плиты) с использованием `InventoryBlockRenderer`. 
  - **Specialized GUI Shader**: Создан `inventory_block` шейдер с трехточечным освещением (Key, Fill, Top) для придания объема и "сочности" иконкам в GUI.
  - **Foliage Color Fix**: Исправлен баг в `ChunkMeshGenerator` — теперь листва и другие тонируемые блоки окрашиваются со всех 6 сторон в инвентаре (убраны серые бока).
  - **Selective 3D**: Сохранена 2D-отрисовка для инструментов и предметов, чтобы сохранить классический вид и читаемость.
  - **Isolation & Scissor**: Внедрена система `glScissor` и очистки буфера глубины для каждого слота, предотвращающая наложение моделей друг на друга.
  - **Hybrid Strict Item Validation (NEW)**:    - Реализован флаг `strict` в `EquipmentComponent`.
    - Мешочек нельзя положить в слот шлема, но можно носить в кармане или аксессуарах.
  - **Advanced Relative GUI Layout (v3.1)**:
    - Внедрена система двухпроходного лейаута (`InventoryLayout`).
    - Поддержка `relativeTo`, `relativeAlign`, `fixedOffsetX/Y`.
  - **Tier 1: Scavenging Expansion**:
    - Добавлена добыча растительных волокон (`plant_fiber`) из высокой травы.
    - [x] Плетение мешочка (`pouch`) из 4 ниток. Мешочек дает +8 слотов в инвентаре.
    - [x] Система автоматического выброса вещей при снятии сумки.
    - **Интерактивное обтёсывание бревен**: превращение бревна в пень с помощью каменного ножа.
      - 16-зонная система прогресса (`carvingMask` в `StumpBlockEntity`).
      - Универсальный шейдер для маскирования текстур для `CarvingRenderer`.
      - Полностью Data-Driven через новый тип рецептов `carving`.
      - **Исправление (v1.1)**: решена проблема "битых текстур" и "двойного клика".
  - **Treecapitator System (Realistic Felling)**:
    - [x] **BFS-based Felling**: Универсальный алгоритм поиска связных компонентов дерева (логи и листва) только выше и вокруг точки удара.
    - [x] **Metadata BIT_NATURAL**: Введен 8-й бит метаданных (0x80) для отличия сгенерированных миром блоков от поставленных игроком.
    - [x] **Universal Felling Stages**: Реализовано 4 технических блока (`felling_stage_1..4`) с уменьшенными хитбоксами (Thinning).
    - [x] **Data-Driven Wood Mapping**: Создан `WoodTypeRegistry` (загрузка из `wood_types.json`) для поддержки множества видов деревьев без дублирования JSON-файлов.
    - [x] **Dynamic Texture Mapping**: `BlockTextureMapper` автоматически подставляет текстуры для стадий на основе тега `minecraft:logs`, игнорируя флаги метаданных через маску `& 0x07`.
    - [x] **Drop Consolidation**: Все ресурсы спавнятся в одной куче у основания дерева.
  - **Flora & Generation Expansion**:
    - [x] **Universal Flora**: Добавлены определения блоков и листвы для березы, ели, акации, джунглей и темного дуба.
    - [x] **Clean Generation**: Из `TreeGenerator` и `OvergrowthStep` полностью удален хардкод `OAK_LOG`. Типы деревьев выбираются динамически из `WoodTypeRegistry`.
  - **Моддинг-движок (Стабилизация)**:
    - ID предметов Tier 1 изменены на 401-404 для предотвращения перезаписи.
- **Моддинг-движок (Core)**:
  - Полностью удален хардкод `BlockType` и `ItemType`.
  - Классы-холдеры `Blocks`, `Items`, `PrefabManager` с авто-инициализацией через Reflection.
  - Внедрен `StructureRegistry` и оптимизировано хранение чанков.
- **Индустрия и Энергия**:
  - Реализован интерфейс `IEnergyStorage`.
  - Добавлены Бензогенератор, Кабели, Лампа и Аккумулятор.
- **Ядро и Графика**:
  - Базовый игровой цикл, OpenGL рендеринг, View Model.
  - Локализация (I18n) и Unicode-шрифты.

- **AAA Locomotion & Impulse Engine (v4.0) (NEW)**:
  - **Fixed-Rate Locomotion (170Hz)**: Decoupled physical locomotion timer (170Hz) from rendering FPS to eliminate micro-jitter and ensure consistent head-bobbing speed.
  - **Cinematic Sneak System**:
    - "Heavy Survival Breath": Synchronized vertical breathing for camera and items in sneak mode, active even when standing still.
    - Velocity-Weighted Blending: Smoothly transitions from vertical-only breathing to horizontal walking wobble based on physical movement intensity.
  - **Dynamic Landing Impulse**: 
    - Quadratic scaling of landing impact based on vertical velocity (V^2).
    - Randomized `mirror` logic: landing shock can tilt the camera left or right randomly for visceral variety.
    - Fast impact recovery (lerp at 12.0f) vs. slow FOV stabilization (lerp at 4.0f).
  - **Atmospheric Fall Tension**:
    - Procedural high-frequency jitter (50Hz) for items during high-altitude falls to simulate wind resistance.
    - Wind drag displacement: items automatically lift (`item_y`) and retract (`item_z`) during terminal velocity descent.
  - **Stability & Precision Fixes**:
    - **Spawn Settle**: Guarded the first animation frame to prevent camera "jumping" or fake landing kicks when joining the world.
    - **Swing Math Fix**: Prevented item "fly-away" bug by applying swing offsets to target variables instead of accumulating directly.
    - **Terminology Standard**: Consolidated all mirroring logic under the `mirror` key across all JSONs and Java classes.
- **Modular Physics & Parkour (v1.0)**:
  - **Data-Driven Physics**: Physical constants (climb duration, grab distance, jump velocity) externalized to `physics.json`.
  - **Clamber & Roll (Cinematic Parkour)**:
    - **Bezier Trajectory**: Implemented a unified 3D Quadratic Bezier path for climbing, ensuring perfect motion smoothness without independent axis jerking.
    - **Cinematic Feedback**: High-fidelity camera system with non-linear Tilt (pitch), dynamic Roll (leaning into movements), and asymmetric FOV pulses.
    - **Effort Simulation**: Added procedural micro-vibrations (shaking) during peak exertion phases of climbing to simulate physical strain.
    - **Randomized Leading Hand**: Every climb randomly selects a `climbSide` multiplier, flipping the camera roll direction for organic visual variety.
  - **Ledge Grabbing Logic**: Advanced raycasting for ledge detection with hitbox-aware positioning to prevent wall clipping.
  - **Physics Stability**: Fixed on-ground detection logic in `Entity.java` to eliminate walk-animation flickering on sloped or complex terrain.
- **Inventory v3 Implementation**: полный переход на Data-Driven GUI и абстракцию хранилищ.
- **CSS Layout Logic**: внедрена поддержка выравнивания `alignX`/`alignY` в GUI.
- **Bugfixes**: исправлены критические ошибки индексов в `Inventory.java`.

## В работе
- **Stamina System**: Implement exhaustion logic for hanging and climbing.
- **Collision Edge Cases**: Refining interactions with corners and slabs.
- **Generic Containers**: Тестирование `ChestScreen` и расширение поддержки сундуков.

## Roadmap
- [x] Milestone 2: Базовые механики выживания (Inventory v3, Hunger, Noise, Parkour v1).
- [ ] Milestone 3: Моддинг-API и Расширяемость (Registry Events, Mod Loader, Physics Registry).
- [ ] Milestone 4: Полноценный геймплей (Мир, Существа, Прогрессия).
